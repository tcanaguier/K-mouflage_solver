KMOUFLAGE — TODO
=================
(liste vivante, à cocher/éditer au fil de l'eau — rien ci-dessous n'est encore fait)
numéro = ordre de priorité 0,1,2,3
## Infra / organisation générale
- [ 1 ] git init + premier commit (rien n'est versionné actuellement — priorité avant de toucher au reste)
- [ 1 ] pyproject.toml (deps numpy/scipy/matplotlib pinnées) + pip install -e .
- [ 3 ] tests unitaires (pytest) sur equations.py (limites analytiques connues : ΛCDM quand beta=0 / K_star->0, etc.)
- [ ? ] CI (GitHub Actions) une fois git + tests + packaging en place
- [ ? ] LICENSE + CITATION.cff (utile pour dépôt Zenodo en marge du papier)

## Bugs / nettoyage repérés
- [ 0 ] growth.py:282 — `gamma_arr = 1.0 + H_conf_prime_arr / H_conf_arr*2` : précédence d'opérateur fausse
      (devrait être /H_conf_arr**2, comme gamma_bg ligne 246 et LCDMGrowth._gamma_vec). Fausse gamma/f/D+
      retournés par KmouflageGrowth.run(). Peut être revoir toute la fonction en soit 
- [ 0 ] verify regarder les quantitées que je trace et ajouter un niveau de verbosité aussi 
- [ 1 ] verify.py:26 — commentaire cassé "4. rho_phi + p_phi = IMPLEmenter."
- [ 1 ] verify.py:143 — TODO oublié en bas de fichier ("Faire main pour tester fonction verif...")
- [ 1 ] verify.py — trop de choses vérifiées/affichées d'un coup ; à recentrer sur moins de quantités
      (l'utilisateur trouve qu'il trace déjà moins de quantités "juste en dessous" dans ses propres notebooks)

## Examples (chantier en cours)
- [ 0 ] redéfinir run_solver via get_or_run (io_utils.py), le renommer/déplacer en run_solver dans utils
- [ 0 ] écrire un guide/notebook "comment créer son propre modèle K(X) et son propre couplage + test potentiel" (extensibilité)
- [ 1 ] niveaux de verbosité 0 / 1 / 2 partout (run(), calibrate_M4_tilde, get_or_run, growth...) —
      définir précisément ce qui s'affiche à chaque niveau
      mais niveau de verbosité générale, c'est à dire 0 = false on affiche rien partout.
      1)
      2)
      3) Je suppose tout temps d'intégration, temps chaque étape, chaque étape en génral
- [ ] revoir verify() : réduire le nombre de quantités vérifiées/tracées, garder l'essentiel


## Finalité
- [ 1 ] retirer tous les commentaires inutiles et les commentaires types IA
- [ 1 ] Ajouter que les commentaires pertinents et utile
- [ 1 ou 2 ] faire un python ou jupyter avec tracer toutes les quantitées importantes et/ou en plusieurs jupyter pour faire comparatif différents models etc
## Plus tard
- [ 3 ] module MCMC (emcee/nautilus ?) branché en boîte noire sur KMouflageBackground/KmouflageGrowth,
      sans toucher au solveur
